PORT=5000
NODE_ENV=development
MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/abhyas
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173

# AI provider (Phase 4+) — Gemini is tried first, Groq (free tier) is the fallback
GEMINI_API_KEY=
GROQ_API_KEY=
OPENAI_API_KEY=
